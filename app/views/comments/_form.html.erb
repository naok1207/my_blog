<%= form_with(model: [@post, @comment], local: true, class: 'c-form') do |f| %>
    <%= f.text_area :content, class: "c-text_area", rows: 5 %>
    <%= button_tag type: "submit", class: "c-submit" do %>
        <i class="far fa-comments"></i> コメントする
    <% end %>
<% end %>